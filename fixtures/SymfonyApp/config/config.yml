imports:
    - { resource: services.yml }

framework:
    secret: test
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
    form: ~
    validation:
        enabled: true
        enable_annotations: true
    session: ~
    test: ~
